diff --git "a/app/Http/Controllers/BookController.php" "b/app/Http/Controllers/BookController.php"
index f11299f..ad69809 100644
--- "a/app/Http/Controllers/BookController.php"
+++ "b/app/Http/Controllers/BookController.php"
@@ -11,16 +11,30 @@ class BookController extends Controller
     public function index(Request $request)
     {
         $search = $request->input('search');
+        $category = $request->input('category');
+
+        $categories = Book::query()
+            ->whereNotNull('category')
+            ->where('category', '!=', '')
+            ->distinct()
+            ->orderBy('category')
+            ->pluck('category');
 
         $books = Book::query()
             ->when($search, function ($query, $search) {
-                $query->where('title', 'like', "%{$search}%")
-                    ->orWhere('author', 'like', "%{$search}%")
-                    ->orWhere('category', 'like', "%{$search}%");
+                $query->where(function ($query) use ($search) {
+                    $query->where('title', 'like', "%{$search}%")
+                        ->orWhere('author', 'like', "%{$search}%")
+                        ->orWhere('category', 'like', "%{$search}%");
+                });
+            })
+            ->when($category, function ($query, $category) {
+                $query->where('category', $category);
             })
+            ->latest()
             ->get();
 
-        return view('books.index', compact('books', 'search'));
+        return view('books.index', compact('books', 'search', 'category', 'categories'));
     }
 
     public function create()
diff --git "a/resources/css/app.css" "b/resources/css/app.css"
index b5c61c9..3d2a978 100644
--- "a/resources/css/app.css"
+++ "b/resources/css/app.css"
@@ -1,3 +1,660 @@
 @tailwind base;
 @tailwind components;
 @tailwind utilities;
+
+@layer base {
+    :root {
+        --ink: #172033;
+        --muted: #667085;
+        --paper: #ffffff;
+        --line: #e5e0d8;
+        --brand: #0f766e;
+        --brand-dark: #115e59;
+        --accent: #c2410c;
+        --danger: #b42318;
+        --shadow: 0 18px 55px rgba(23, 32, 51, 0.1);
+    }
+
+    * {
+        box-sizing: border-box;
+    }
+
+    body {
+        margin: 0;
+        min-height: 100vh;
+        color: var(--ink);
+        background:
+            linear-gradient(180deg, rgba(245, 242, 236, 0.92), rgba(250, 250, 249, 1) 420px),
+            #fafaf9;
+        font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+    }
+
+    a {
+        color: inherit;
+    }
+
+    img {
+        max-width: 100%;
+    }
+
+    h1,
+    h2,
+    p {
+        margin-top: 0;
+    }
+
+    h1 {
+        margin-bottom: 0;
+        font-size: clamp(34px, 5vw, 56px);
+        line-height: 1;
+        letter-spacing: 0;
+    }
+}
+
+@layer components {
+    .library-shell {
+        min-height: 100vh;
+    }
+
+    .site-nav {
+        position: sticky;
+        top: 0;
+        z-index: 10;
+        border-bottom: 1px solid rgba(229, 224, 216, 0.82);
+        background: rgba(255, 255, 255, 0.86);
+        backdrop-filter: blur(18px);
+    }
+
+    .site-nav__inner {
+        display: flex;
+        align-items: center;
+        justify-content: space-between;
+        gap: 18px;
+        width: min(1180px, calc(100% - 32px));
+        margin: 0 auto;
+        padding: 16px 0;
+    }
+
+    .brand,
+    .site-nav__links,
+    .toolbar,
+    .toolbar__actions,
+    .card-actions,
+    .detail-actions,
+    .form-actions,
+    .welcome-actions,
+    .current-cover {
+        display: flex;
+        align-items: center;
+    }
+
+    .brand {
+        gap: 10px;
+        text-decoration: none;
+        font-weight: 800;
+    }
+
+    .brand__mark {
+        display: grid;
+        width: 38px;
+        height: 38px;
+        place-items: center;
+        border-radius: 8px;
+        color: #fff;
+        background: var(--brand);
+        box-shadow: 0 10px 24px rgba(15, 118, 110, 0.22);
+    }
+
+    .brand__text {
+        font-size: 19px;
+        letter-spacing: 0;
+    }
+
+    .site-nav__links {
+        gap: 8px;
+        flex-wrap: wrap;
+        justify-content: flex-end;
+    }
+
+    .nav-link {
+        border-radius: 8px;
+        padding: 10px 12px;
+        color: var(--muted);
+        font-size: 14px;
+        font-weight: 700;
+        text-decoration: none;
+    }
+
+    .nav-link:hover,
+    .nav-link.is-active {
+        color: var(--ink);
+        background: #f2eee7;
+    }
+
+    .page-wrap {
+        width: min(1180px, calc(100% - 32px));
+        margin: 0 auto;
+        padding: 34px 0 56px;
+    }
+
+    .page-hero,
+    .welcome-hero,
+    .form-page {
+        display: grid;
+        gap: 24px;
+    }
+
+    .page-hero {
+        grid-template-columns: minmax(0, 1fr) auto;
+        align-items: end;
+        margin-bottom: 24px;
+    }
+
+    .eyebrow {
+        margin: 0 0 8px;
+        color: var(--accent);
+        font-size: 12px;
+        font-weight: 800;
+        letter-spacing: 0.08em;
+        text-transform: uppercase;
+    }
+
+    .toolbar {
+        gap: 14px;
+        align-items: end;
+        margin-bottom: 26px;
+        padding: 16px;
+        border: 1px solid var(--line);
+        border-radius: 8px;
+        background: rgba(255, 255, 255, 0.9);
+        box-shadow: var(--shadow);
+    }
+
+    .field {
+        display: grid;
+        gap: 8px;
+        width: 100%;
+    }
+
+    .field--search {
+        flex: 1 1 420px;
+    }
+
+    .field--select {
+        flex: 0 1 240px;
+    }
+
+    .field__label {
+        color: #344054;
+        font-size: 13px;
+        font-weight: 800;
+    }
+
+    input,
+    select,
+    textarea {
+        width: 100%;
+        border: 1px solid #d6d0c8;
+        border-radius: 8px;
+        background: #fff;
+        color: var(--ink);
+        font: inherit;
+        outline: none;
+        transition: border-color 0.18s ease, box-shadow 0.18s ease;
+    }
+
+    input,
+    select {
+        min-height: 46px;
+        padding: 0 13px;
+    }
+
+    textarea {
+        min-height: 180px;
+        padding: 13px;
+        resize: vertical;
+    }
+
+    input:focus,
+    select:focus,
+    textarea:focus {
+        border-color: var(--brand);
+        box-shadow: 0 0 0 4px rgba(15, 118, 110, 0.12);
+    }
+
+    .button {
+        display: inline-flex;
+        min-height: 44px;
+        align-items: center;
+        justify-content: center;
+        gap: 8px;
+        border: 1px solid transparent;
+        border-radius: 8px;
+        padding: 0 16px;
+        font-weight: 800;
+        line-height: 1;
+        text-align: center;
+        text-decoration: none;
+        cursor: pointer;
+        transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
+    }
+
+    .button:hover {
+        transform: translateY(-1px);
+    }
+
+    .button--sm {
+        min-height: 38px;
+        padding-inline: 13px;
+        font-size: 14px;
+    }
+
+    .button--primary {
+        color: #fff;
+        background: var(--brand);
+    }
+
+    .button--primary:hover {
+        background: var(--brand-dark);
+    }
+
+    .button--ghost,
+    .button--soft {
+        color: var(--ink);
+        background: #f5f2ec;
+        border-color: #e4ded5;
+    }
+
+    .button--danger {
+        color: #fff;
+        background: var(--danger);
+    }
+
+    .books-grid {
+        display: grid;
+        grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
+        gap: 22px;
+    }
+
+    .book-card,
+    .form-card,
+    .empty-state,
+    .detail-panel {
+        border: 1px solid var(--line);
+        border-radius: 8px;
+        background: var(--paper);
+        box-shadow: var(--shadow);
+    }
+
+    .book-card {
+        overflow: hidden;
+    }
+
+    .book-cover,
+    .detail-cover {
+        display: grid;
+        place-items: center;
+        overflow: hidden;
+        background:
+            linear-gradient(135deg, rgba(15, 118, 110, 0.14), rgba(194, 65, 12, 0.12)),
+            #ebe5dc;
+        color: #475467;
+        text-align: center;
+        text-decoration: none;
+        font-weight: 800;
+    }
+
+    .book-cover {
+        aspect-ratio: 3 / 4;
+        padding: 22px;
+    }
+
+    .book-cover img,
+    .detail-cover img {
+        width: 100%;
+        height: 100%;
+        object-fit: cover;
+    }
+
+    .book-card__body {
+        display: grid;
+        gap: 10px;
+        padding: 16px;
+    }
+
+    .book-card__title {
+        color: var(--ink);
+        font-size: 20px;
+        font-weight: 850;
+        line-height: 1.18;
+        text-decoration: none;
+    }
+
+    .book-card__title:hover,
+    .back-link:hover {
+        color: var(--brand);
+    }
+
+    .book-card__meta {
+        margin-bottom: 4px;
+        color: var(--muted);
+    }
+
+    .pill {
+        width: fit-content;
+        border: 1px solid #cce1dc;
+        border-radius: 999px;
+        padding: 5px 10px;
+        color: var(--brand-dark);
+        background: #edf7f4;
+        font-size: 12px;
+        font-weight: 850;
+    }
+
+    .card-actions {
+        gap: 8px;
+        margin-top: 6px;
+    }
+
+    .card-actions .button,
+    .card-actions form {
+        flex: 1;
+    }
+
+    .card-actions form,
+    .detail-actions form {
+        margin: 0;
+    }
+
+    .card-actions .button {
+        width: 100%;
+    }
+
+    .empty-state {
+        display: grid;
+        place-items: center;
+        gap: 10px;
+        padding: 44px 22px;
+        text-align: center;
+    }
+
+    .empty-state h2 {
+        margin: 0;
+        font-size: 26px;
+    }
+
+    .empty-state p {
+        color: var(--muted);
+    }
+
+    .detail-layout {
+        display: grid;
+        grid-template-columns: minmax(280px, 420px) minmax(0, 1fr);
+        gap: 28px;
+        align-items: start;
+    }
+
+    .detail-cover {
+        min-height: 600px;
+        border-radius: 8px;
+        box-shadow: var(--shadow);
+        font-size: 26px;
+        line-height: 1.15;
+        padding: 28px;
+    }
+
+    .detail-panel {
+        display: grid;
+        gap: 18px;
+        padding: 34px;
+    }
+
+    .back-link {
+        color: var(--muted);
+        font-size: 14px;
+        font-weight: 800;
+        text-decoration: none;
+    }
+
+    .detail-author {
+        color: var(--muted);
+        font-size: 20px;
+    }
+
+    .detail-meta {
+        display: grid;
+        grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
+        gap: 10px;
+    }
+
+    .detail-meta div {
+        display: grid;
+        gap: 4px;
+        border: 1px solid var(--line);
+        border-radius: 8px;
+        padding: 12px;
+        background: #fbfaf8;
+    }
+
+    .detail-meta span {
+        color: var(--muted);
+        font-size: 12px;
+        font-weight: 800;
+        text-transform: uppercase;
+    }
+
+    .description-block {
+        border-top: 1px solid var(--line);
+        padding-top: 22px;
+    }
+
+    .description-block h2 {
+        margin-bottom: 10px;
+        font-size: 19px;
+    }
+
+    .description-block p {
+        color: #344054;
+        line-height: 1.75;
+        white-space: pre-line;
+    }
+
+    .detail-actions {
+        gap: 10px;
+        flex-wrap: wrap;
+    }
+
+    .form-page {
+        grid-template-columns: minmax(220px, 340px) minmax(0, 1fr);
+        align-items: start;
+    }
+
+    .form-intro {
+        position: sticky;
+        top: 92px;
+    }
+
+    .form-card {
+        padding: 24px;
+    }
+
+    .book-form,
+    .form-grid {
+        display: grid;
+        gap: 18px;
+    }
+
+    .form-grid {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
+
+    .field__error {
+        color: var(--danger);
+        font-size: 13px;
+        font-weight: 700;
+    }
+
+    .alert {
+        margin-bottom: 18px;
+        border-radius: 8px;
+        padding: 14px 16px;
+    }
+
+    .alert--error {
+        color: #7a271a;
+        background: #ffebe7;
+        border: 1px solid #fecdca;
+    }
+
+    .alert ul {
+        margin: 8px 0 0;
+        padding-left: 20px;
+    }
+
+    .current-cover {
+        gap: 14px;
+        border: 1px solid var(--line);
+        border-radius: 8px;
+        padding: 12px;
+        background: #fbfaf8;
+    }
+
+    .current-cover img {
+        width: 72px;
+        height: 96px;
+        border-radius: 6px;
+        object-fit: cover;
+    }
+
+    .current-cover span {
+        display: block;
+        color: var(--muted);
+        font-size: 13px;
+        font-weight: 800;
+    }
+
+    .form-actions {
+        justify-content: flex-end;
+        gap: 10px;
+        border-top: 1px solid var(--line);
+        padding-top: 18px;
+    }
+
+    .welcome-hero {
+        min-height: calc(100vh - 180px);
+        align-items: center;
+    }
+
+    .welcome-hero__content {
+        max-width: 720px;
+    }
+
+    .welcome-hero h1 {
+        max-width: 680px;
+    }
+
+    .welcome-hero p:not(.eyebrow) {
+        max-width: 560px;
+        margin: 18px 0 28px;
+        color: var(--muted);
+        font-size: 20px;
+        line-height: 1.55;
+    }
+
+    .welcome-actions,
+    .toolbar__actions {
+        gap: 10px;
+    }
+}
+
+@layer utilities {
+    @media (max-width: 820px) {
+        .site-nav__inner,
+        .page-hero,
+        .toolbar,
+        .detail-layout,
+        .form-page {
+            grid-template-columns: 1fr;
+        }
+
+        .site-nav__inner,
+        .toolbar {
+            align-items: stretch;
+        }
+
+        .site-nav__inner,
+        .site-nav__links,
+        .toolbar,
+        .toolbar__actions,
+        .form-actions,
+        .welcome-actions {
+            flex-direction: column;
+        }
+
+        .site-nav__links,
+        .toolbar__actions,
+        .form-actions,
+        .welcome-actions {
+            align-items: stretch;
+        }
+
+        .page-wrap {
+            padding-top: 24px;
+        }
+
+        .page-hero {
+            align-items: start;
+        }
+
+        .page-hero .button,
+        .toolbar .button,
+        .form-actions .button,
+        .welcome-actions .button {
+            width: 100%;
+        }
+
+        .detail-cover {
+            min-height: 0;
+            aspect-ratio: 3 / 4;
+        }
+
+        .detail-panel,
+        .form-card {
+            padding: 22px;
+        }
+
+        .form-intro {
+            position: static;
+        }
+
+        .form-grid {
+            grid-template-columns: 1fr;
+        }
+    }
+
+    @media (max-width: 520px) {
+        .site-nav__inner,
+        .page-wrap {
+            width: min(100% - 22px, 1180px);
+        }
+
+        h1 {
+            font-size: 34px;
+        }
+
+        .books-grid {
+            grid-template-columns: 1fr;
+        }
+
+        .card-actions,
+        .detail-actions {
+            align-items: stretch;
+            flex-direction: column;
+        }
+
+        .detail-actions .button,
+        .detail-actions form {
+            width: 100%;
+        }
+    }
+}
diff --git "a/resources/views/books/create.blade.php" "b/resources/views/books/create.blade.php"
index 4219ae5..7d160f8 100644
--- "a/resources/views/books/create.blade.php"
+++ "b/resources/views/books/create.blade.php"
@@ -1,126 +1,29 @@
-<!DOCTYPE html>
-<html>
-<head>
-    <title>Add New Book</title>
+@extends('layouts.library', ['title' => 'Add Book | Readwise'])
 
-    <style>
-        body{
-            font-family:Arial,sans-serif;
-            background:#f4f6f9;
-            padding:40px;
-        }
-
-        .container{
-            max-width:700px;
-            margin:auto;
-            background:white;
-            padding:30px;
-            border-radius:15px;
-            box-shadow:0 4px 15px rgba(0,0,0,0.1);
-        }
-
-        h1{
-            text-align:center;
-            margin-bottom:25px;
-        }
-
-        label{
-            display:block;
-            margin-top:15px;
-            margin-bottom:5px;
-            font-weight:bold;
-        }
-
-        input,
-        textarea,
-        select{
-            width:100%;
-            padding:12px;
-            border:1px solid #ccc;
-            border-radius:8px;
-            box-sizing:border-box;
-        }
-
-        textarea{
-            resize:vertical;
-        }
-
-        button{
-            width:100%;
-            margin-top:20px;
-            padding:12px;
-            background:#2563eb;
-            color:white;
-            border:none;
-            border-radius:8px;
-            cursor:pointer;
-            font-size:16px;
-        }
-
-        button:hover{
-            background:#1d4ed8;
-        }
-
-        .error{
-            color:red;
-            margin-bottom:10px;
-        }
-    </style>
-</head>
-<body>
-
-<div class="container">
-
-    <h1>📚 Add New Book</h1>
-
-    @if ($errors->any())
-        <div class="error">
-            <ul>
-                @foreach ($errors->all() as $error)
-                    <li>{{ $error }}</li>
-                @endforeach
-            </ul>
+@section('content')
+    <section class="form-page">
+        <div class="form-intro">
+            <p class="eyebrow">New Book</p>
+            <h1>Add a book to your library.</h1>
         </div>
-    @endif
-
-    <form action="/books" method="POST" enctype="multipart/form-data">
-        @csrf
-
-        <label>Book Title</label>
-        <input type="text" name="title" required>
-
-        <label>Author</label>
-        <input type="text" name="author" required>
-
-        <label>Description</label>
-        <textarea name="description" rows="5"></textarea>
 
-        <label>Category</label>
-        <select name="category">
-            <option value="">Select Category</option>
-            <option value="Fiction">Fiction</option>
-            <option value="Non-Fiction">Non-Fiction</option>
-            <option value="Science">Science</option>
-            <option value="Technology">Technology</option>
-            <option value="Business">Business</option>
-            <option value="Self-Help">Self-Help</option>
-            <option value="Biography">Biography</option>
-            <option value="History">History</option>
-            <option value="Education">Education</option>
-            <option value="Fantasy">Fantasy</option>
-            <option value="Mystery">Mystery</option>
-            <option value="Romance">Romance</option>
-        </select>
-
-        <label>Book Cover Image</label>
-        <input type="file" name="cover_image" accept="image/*">
-
-        <button type="submit">
-            Save Book
-        </button>
-    </form>
-
-</div>
-
-</body>
-</html>
\ No newline at end of file
+        <div class="form-card">
+            @if ($errors->any())
+                <div class="alert alert--error">
+                    <strong>Please check the form.</strong>
+                    <ul>
+                        @foreach ($errors->all() as $error)
+                            <li>{{ $error }}</li>
+                        @endforeach
+                    </ul>
+                </div>
+            @endif
+
+            <form action="/books" method="POST" enctype="multipart/form-data" class="book-form">
+                @csrf
+
+                @include('books.partials.form', ['book' => null, 'submitLabel' => 'Save Book'])
+            </form>
+        </div>
+    </section>
+@endsection
diff --git "a/resources/views/books/edit.blade.php" "b/resources/views/books/edit.blade.php"
index 6dae0ff..7e71d11 100644
--- "a/resources/views/books/edit.blade.php"
+++ "b/resources/views/books/edit.blade.php"
@@ -1,112 +1,30 @@
-<!DOCTYPE html>
-<html>
-<head>
-    <title>Edit Book</title>
-
-    <style>
-        body{
-            font-family: Arial, sans-serif;
-            background:#f4f6f9;
-            margin:0;
-            padding:40px;
-        }
-
-        .container{
-            max-width:600px;
-            margin:auto;
-        }
-
-        .card{
-            background:white;
-            padding:30px;
-            border-radius:15px;
-            box-shadow:0 4px 15px rgba(0,0,0,0.1);
-        }
-
-        h1{
-            text-align:center;
-            color:#333;
-        }
-
-        label{
-            display:block;
-            margin-bottom:5px;
-        }
-
-        input{
-            width:100%;
-            padding:12px;
-            margin-bottom:8px;
-            border:1px solid #ddd;
-            border-radius:8px;
-            box-sizing:border-box;
-        }
-
-        .error{
-            color:#dc2626;
-            font-size:14px;
-            margin-bottom:16px;
-        }
-
-        button{
-            width:100%;
-            padding:12px;
-            border:none;
-            border-radius:8px;
-            background:#2563eb;
-            color:white;
-            font-size:16px;
-            cursor:pointer;
-        }
-
-        button:hover{
-            background:#1d4ed8;
-        }
-
-        a{
-            display:inline-block;
-            margin-top:20px;
-            color:#2563eb;
-            text-decoration:none;
-        }
-    </style>
-</head>
-
-<body>
-
-<div class="container">
-
-    <h1>Edit Book</h1>
-
-    <div class="card">
-
-        <form method="POST" action="/books/{{ $book->id }}">
-            @csrf
-            @method('PUT')
-
-            <label>Book Title</label>
-            <input type="text" name="title" value="{{ old('title', $book->title) }}">
-            @error('title')
-                <div class="error">{{ $message }}</div>
-            @enderror
-
-            <label>Author</label>
-            <input type="text" name="author" value="{{ old('author', $book->author) }}">
-            @error('author')
-                <div class="error">{{ $message }}</div>
-            @enderror
-
-            <button type="submit">
-                Update Book
-            </button>
-
-        </form>
-
-        <a href="/books">Back to Books</a>
-
-    </div>
-
-</div>
-
-</body>
-</html>
+@extends('layouts.library', ['title' => 'Edit Book | Readwise'])
+
+@section('content')
+    <section class="form-page">
+        <div class="form-intro">
+            <p class="eyebrow">Edit Book</p>
+            <h1>Update {{ $book->title }}.</h1>
+        </div>
+
+        <div class="form-card">
+            @if ($errors->any())
+                <div class="alert alert--error">
+                    <strong>Please check the form.</strong>
+                    <ul>
+                        @foreach ($errors->all() as $error)
+                            <li>{{ $error }}</li>
+                        @endforeach
+                    </ul>
+                </div>
+            @endif
+
+            <form method="POST" action="/books/{{ $book->id }}" enctype="multipart/form-data" class="book-form">
+                @csrf
+                @method('PUT')
+
+                @include('books.partials.form', ['book' => $book, 'submitLabel' => 'Update Book'])
+            </form>
+        </div>
+    </section>
+@endsection
diff --git "a/resources/views/books/index.blade.php" "b/resources/views/books/index.blade.php"
index b1ba8c1..a289595 100644
--- "a/resources/views/books/index.blade.php"
+++ "b/resources/views/books/index.blade.php"
@@ -1,312 +1,80 @@
-<!DOCTYPE html>
-<html>
-<head>
-    <title>Books</title>
+@extends('layouts.library', ['title' => 'Readwise Library'])
 
-    <style>
-        *{
-            box-sizing:border-box;
-        }
-
-        body{
-            font-family:Arial,sans-serif;
-            background:#f4f6f9;
-            margin:0;
-            padding:40px;
-            color:#1f2937;
-        }
-
-        .page{
-            max-width:1100px;
-            margin:auto;
-        }
-
-        .page-header{
-            display:flex;
-            justify-content:space-between;
-            align-items:center;
-            gap:16px;
-            margin-bottom:24px;
-        }
-
-        h1{
-            margin:0;
-            color:#111827;
-            font-size:32px;
-        }
-
-        .add-link{
-            background:#2563eb;
-            color:white;
-            padding:10px 16px;
-            border-radius:8px;
-            text-decoration:none;
-            font-weight:bold;
-        }
-
-        .add-link:hover{
-            background:#1d4ed8;
-        }
-
-        .search-form{
-            display:flex;
-            gap:10px;
-            margin-bottom:28px;
-            background:white;
-            padding:16px;
-            border-radius:12px;
-            box-shadow:0 4px 15px rgba(0,0,0,0.08);
-        }
-
-        .search-input{
-            flex:1;
-            min-width:0;
-            border:1px solid #d1d5db;
-            border-radius:8px;
-            padding:12px 14px;
-            font-size:15px;
-        }
-
-        .search-button,
-        .clear-link{
-            border:none;
-            border-radius:8px;
-            padding:12px 16px;
-            font-size:15px;
-            font-weight:bold;
-            cursor:pointer;
-            text-decoration:none;
-            white-space:nowrap;
-        }
-
-        .search-button{
-            background:#2563eb;
-            color:white;
-        }
-
-        .search-button:hover{
-            background:#1d4ed8;
-        }
-
-        .clear-link{
-            background:#f3f4f6;
-            color:#111827;
-        }
-
-        .clear-link:hover{
-            background:#e5e7eb;
-        }
-
-        .books-grid{
-            display:grid;
-            grid-template-columns:repeat(auto-fit,minmax(240px,1fr));
-            gap:22px;
-        }
-
-        .book-card{
-            background:white;
-            border-radius:12px;
-            overflow:hidden;
-            box-shadow:0 4px 15px rgba(0,0,0,0.08);
-        }
-
-        .cover{
-            width:100%;
-            aspect-ratio:3/4;
-            background:#e5e7eb;
-            display:flex;
-            align-items:center;
-            justify-content:center;
-            color:#6b7280;
-            font-weight:bold;
-        }
-
-        .cover img{
-            width:100%;
-            height:100%;
-            object-fit:cover;
-            display:block;
-        }
-
-        .book-content{
-            padding:18px;
-        }
-
-        .book-title{
-            display:block;
-            text-decoration:none;
-            color:#111827;
-            font-size:20px;
-            font-weight:bold;
-            margin-bottom:10px;
-            line-height:1.3;
-        }
-
-        .book-title:hover{
-            color:#2563eb;
-        }
-
-        .meta{
-            color:#4b5563;
-            line-height:1.4;
-            margin:6px 0;
-        }
-
-        .category{
-            display:inline-block;
-            margin-top:8px;
-            padding:5px 10px;
-            border-radius:999px;
-            background:#eef2ff;
-            color:#3730a3;
-            font-size:13px;
-            font-weight:bold;
-        }
-
-        .actions{
-            display:flex;
-            gap:10px;
-            margin-top:18px;
-        }
-
-        .edit-button{
-            flex:1;
-            background:#f3f4f6;
-            color:#111827;
-            text-decoration:none;
-            text-align:center;
-            padding:10px;
-            border-radius:8px;
-            font-weight:bold;
-        }
-
-        .edit-button:hover{
-            background:#e5e7eb;
-        }
-
-        .delete-form{
-            flex:1;
-            margin:0;
-        }
-
-        .delete-button{
-            width:100%;
-            border:none;
-            background:#dc2626;
-            color:white;
-            padding:10px;
-            border-radius:8px;
-            cursor:pointer;
-            font-weight:bold;
-        }
-
-        .delete-button:hover{
-            background:#b91c1c;
-        }
-
-        .empty-state{
-            background:white;
-            border-radius:12px;
-            padding:30px;
-            text-align:center;
-            color:#6b7280;
-            box-shadow:0 4px 15px rgba(0,0,0,0.08);
-        }
-
-        @media (max-width:650px){
-            body{
-                padding:20px;
-            }
-
-            .page-header,
-            .search-form{
-                flex-direction:column;
-                align-items:stretch;
-            }
-
-            h1{
-                font-size:28px;
-            }
-        }
-    </style>
-</head>
-<body>
-
-<div class="page">
-
-    <div class="page-header">
-        <h1>Books</h1>
-        <a class="add-link" href="/books/create">Add Book</a>
-    </div>
-
-    <form class="search-form" method="GET" action="/books">
-        <input
-            class="search-input"
-            type="text"
-            name="search"
-            value="{{ $search }}"
-            placeholder="Search by title, author, or category"
-        >
-
-        <button class="search-button" type="submit">Search</button>
-
-        @if($search)
-            <a class="clear-link" href="/books">Clear</a>
-        @endif
+@section('content')
+    <section class="page-hero">
+        <div>
+            <p class="eyebrow">Personal Library</p>
+            <h1>Your books, beautifully organized.</h1>
+        </div>
+        <a class="button button--primary" href="/books/create">Add Book</a>
+    </section>
+
+    <form class="toolbar" method="GET" action="/books">
+        <label class="field field--search">
+            <span class="field__label">Search</span>
+            <input type="search" name="search" value="{{ $search }}" placeholder="Search by title, author, or category">
+        </label>
+
+        <label class="field field--select">
+            <span class="field__label">Category</span>
+            <select name="category">
+                <option value="">All categories</option>
+                @foreach($categories as $categoryOption)
+                    <option value="{{ $categoryOption }}" @selected($category === $categoryOption)>
+                        {{ $categoryOption }}
+                    </option>
+                @endforeach
+            </select>
+        </label>
+
+        <div class="toolbar__actions">
+            <button class="button button--primary" type="submit">Search</button>
+            @if($search || $category)
+                <a class="button button--ghost" href="/books">Clear</a>
+            @endif
+        </div>
     </form>
 
     @if($books->isEmpty())
-        <div class="empty-state">
-            @if($search)
-                No books found for "{{ $search }}".
-            @else
-                No books have been added yet.
-            @endif
-        </div>
+        <section class="empty-state">
+            <h2>No books found</h2>
+            <p>
+                @if($search || $category)
+                    Try a different search or category.
+                @else
+                    Start your library by adding your first book.
+                @endif
+            </p>
+            <a class="button button--primary" href="/books/create">Add Book</a>
+        </section>
     @else
-        <div class="books-grid">
+        <section class="books-grid" aria-label="Book library">
             @foreach($books as $book)
-                <div class="book-card">
-                    <a class="cover" href="/books/{{ $book->id }}">
+                <article class="book-card">
+                    <a class="book-cover" href="/books/{{ $book->id }}" aria-label="View {{ $book->title }}">
                         @if($book->cover_image)
                             <img src="{{ asset('storage/' . $book->cover_image) }}" alt="{{ $book->title }} cover">
                         @else
-                            No Cover
+                            <span>{{ \Illuminate\Support\Str::limit($book->title, 34) }}</span>
                         @endif
                     </a>
 
-                    <div class="book-content">
-                        <a class="book-title" href="/books/{{ $book->id }}">
-                            {{ $book->title }}
-                        </a>
-
-                        <p class="meta">
-                            Author: {{ $book->author ?: 'Unknown' }}
-                        </p>
+                    <div class="book-card__body">
+                        <span class="pill">{{ $book->category ?: 'Uncategorized' }}</span>
+                        <a class="book-card__title" href="/books/{{ $book->id }}">{{ $book->title }}</a>
+                        <p class="book-card__meta">{{ $book->author ?: 'Unknown author' }}</p>
 
-                        <span class="category">
-                            {{ $book->category ?: 'Uncategorized' }}
-                        </span>
-
-                        <div class="actions">
-                            <a class="edit-button" href="/books/{{ $book->id }}/edit">Edit</a>
-
-                            <form class="delete-form" method="POST" action="/books/{{ $book->id }}">
+                        <div class="card-actions">
+                            <a class="button button--soft" href="/books/{{ $book->id }}/edit">Edit</a>
+                            <form method="POST" action="/books/{{ $book->id }}">
                                 @csrf
                                 @method('DELETE')
-
-                                <button class="delete-button" type="submit" onclick="return confirm('Delete this book?')">
-                                    Delete
-                                </button>
+                                <button class="button button--danger" type="submit" onclick="return confirm('Delete this book?')">Delete</button>
                             </form>
                         </div>
                     </div>
-                </div>
+                </article>
             @endforeach
-        </div>
+        </section>
     @endif
-
-</div>
-
-</body>
-</html>
+@endsection
diff --git "a/resources/views/books/partials/form.blade.php" "b/resources/views/books/partials/form.blade.php"
new file mode 100644
index 0000000..d48af5d
--- /dev/null
+++ "b/resources/views/books/partials/form.blade.php"
@@ -0,0 +1,80 @@
+@php
+    $categoryOptions = [
+        'Fiction',
+        'Non-Fiction',
+        'Science',
+        'Technology',
+        'Business',
+        'Self-Help',
+        'Biography',
+        'History',
+        'Education',
+        'Fantasy',
+        'Mystery',
+        'Romance',
+    ];
+@endphp
+
+<div class="form-grid">
+    <label class="field">
+        <span class="field__label">Book Title</span>
+        <input type="text" name="title" value="{{ old('title', optional($book)->title) }}" required>
+        @error('title')
+            <span class="field__error">{{ $message }}</span>
+        @enderror
+    </label>
+
+    <label class="field">
+        <span class="field__label">Author</span>
+        <input type="text" name="author" value="{{ old('author', optional($book)->author) }}" required>
+        @error('author')
+            <span class="field__error">{{ $message }}</span>
+        @enderror
+    </label>
+
+    <label class="field">
+        <span class="field__label">Category</span>
+        <select name="category">
+            <option value="">Select Category</option>
+            @foreach($categoryOptions as $categoryOption)
+                <option value="{{ $categoryOption }}" @selected(old('category', optional($book)->category) === $categoryOption)>
+                    {{ $categoryOption }}
+                </option>
+            @endforeach
+        </select>
+        @error('category')
+            <span class="field__error">{{ $message }}</span>
+        @enderror
+    </label>
+
+    <label class="field">
+        <span class="field__label">Book Cover Image</span>
+        <input type="file" name="cover_image" accept="image/*">
+        @error('cover_image')
+            <span class="field__error">{{ $message }}</span>
+        @enderror
+    </label>
+</div>
+
+@if(optional($book)->cover_image)
+    <div class="current-cover">
+        <img src="{{ asset('storage/' . $book->cover_image) }}" alt="{{ $book->title }} cover">
+        <div>
+            <span>Current cover</span>
+            <strong>{{ $book->title }}</strong>
+        </div>
+    </div>
+@endif
+
+<label class="field">
+    <span class="field__label">Description</span>
+    <textarea name="description" rows="7">{{ old('description', optional($book)->description) }}</textarea>
+    @error('description')
+        <span class="field__error">{{ $message }}</span>
+    @enderror
+</label>
+
+<div class="form-actions">
+    <a class="button button--ghost" href="/books">Cancel</a>
+    <button class="button button--primary" type="submit">{{ $submitLabel }}</button>
+</div>
diff --git "a/resources/views/books/show.blade.php" "b/resources/views/books/show.blade.php"
index 490cecf..ed8fe50 100644
--- "a/resources/views/books/show.blade.php"
+++ "b/resources/views/books/show.blade.php"
@@ -1,227 +1,57 @@
-<!DOCTYPE html>
-<html>
-<head>
-    <title>{{ $book->title }}</title>
+@extends('layouts.library', ['title' => $book->title . ' | Readwise'])
 
-    <style>
-        *{
-            box-sizing:border-box;
-        }
-
-        body{
-            font-family:Arial,sans-serif;
-            background:#f4f6f9;
-            margin:0;
-            padding:40px;
-            color:#1f2937;
-        }
-
-        .page{
-            max-width:1100px;
-            margin:auto;
-        }
-
-        .details-card{
-            display:grid;
-            grid-template-columns:minmax(260px,360px) 1fr;
-            gap:34px;
-            background:white;
-            border-radius:12px;
-            overflow:hidden;
-            box-shadow:0 4px 15px rgba(0,0,0,0.08);
-        }
-
-        .cover-panel{
-            background:#e5e7eb;
-            min-height:480px;
-            display:flex;
-            align-items:center;
-            justify-content:center;
-            color:#6b7280;
-            font-size:22px;
-            font-weight:bold;
-        }
-
-        .cover-panel img{
-            width:100%;
-            height:100%;
-            object-fit:cover;
-            display:block;
-        }
-
-        .book-details{
-            padding:34px 34px 34px 0;
-        }
-
-        h1{
-            color:#111827;
-            font-size:36px;
-            line-height:1.2;
-            margin:0 0 14px;
-        }
-
-        .author{
-            color:#4b5563;
-            font-size:18px;
-            margin:0 0 14px;
-        }
-
-        .category{
-            display:inline-block;
-            margin-bottom:26px;
-            padding:6px 12px;
-            border-radius:999px;
-            background:#eef2ff;
-            color:#3730a3;
-            font-size:14px;
-            font-weight:bold;
-        }
-
-        .section-title{
-            color:#111827;
-            font-size:18px;
-            margin:0 0 10px;
-        }
-
-        .description{
-            color:#374151;
-            line-height:1.7;
-            margin:0;
-            white-space:pre-line;
-        }
-
-        .actions{
-            display:flex;
-            gap:12px;
-            margin-top:30px;
-            flex-wrap:wrap;
-        }
-
-        .button,
-        .delete-button{
-            border:none;
-            border-radius:8px;
-            padding:11px 16px;
-            font-size:15px;
-            font-weight:bold;
-            cursor:pointer;
-            text-decoration:none;
-            text-align:center;
-        }
-
-        .back-button{
-            background:#2563eb;
-            color:white;
-        }
-
-        .back-button:hover{
-            background:#1d4ed8;
-        }
-
-        .edit-button{
-            background:#f3f4f6;
-            color:#111827;
-        }
-
-        .edit-button:hover{
-            background:#e5e7eb;
-        }
-
-        .delete-form{
-            margin:0;
-        }
-
-        .delete-button{
-            background:#dc2626;
-            color:white;
-        }
-
-        .delete-button:hover{
-            background:#b91c1c;
-        }
-
-        @media (max-width:800px){
-            body{
-                padding:20px;
-            }
-
-            .details-card{
-                grid-template-columns:1fr;
-                gap:0;
-            }
-
-            .cover-panel{
-                min-height:0;
-                aspect-ratio:3/4;
-            }
-
-            .book-details{
-                padding:24px;
-            }
-
-            h1{
-                font-size:30px;
-            }
-
-            .actions{
-                flex-direction:column;
-            }
-
-            .button,
-            .delete-button{
-                width:100%;
-            }
-        }
-    </style>
-</head>
-<body>
-
-<div class="page">
-
-    <div class="details-card">
-        <div class="cover-panel">
+@section('content')
+    <article class="detail-layout">
+        <div class="detail-cover">
             @if($book->cover_image)
                 <img src="{{ asset('storage/' . $book->cover_image) }}" alt="{{ $book->title }} cover">
             @else
-                No Cover
+                <span>{{ $book->title }}</span>
             @endif
         </div>
 
-        <div class="book-details">
+        <div class="detail-panel">
+            <a class="back-link" href="/books">Back to Library</a>
+            <span class="pill">{{ $book->category ?: 'Uncategorized' }}</span>
             <h1>{{ $book->title }}</h1>
+            <p class="detail-author">{{ $book->author ?: 'Unknown author' }}</p>
+
+            <div class="detail-meta">
+                @if(!empty($book->published_year))
+                    <div>
+                        <span>Published</span>
+                        <strong>{{ $book->published_year }}</strong>
+                    </div>
+                @endif
+
+                @if(!empty($book->isbn))
+                    <div>
+                        <span>ISBN</span>
+                        <strong>{{ $book->isbn }}</strong>
+                    </div>
+                @endif
+
+                @if(!is_null($book->average_rating))
+                    <div>
+                        <span>Rating</span>
+                        <strong>{{ $book->average_rating }}</strong>
+                    </div>
+                @endif
+            </div>
 
-            <p class="author">
-                Author: {{ $book->author ?: 'Unknown' }}
-            </p>
-
-            <span class="category">
-                {{ $book->category ?: 'Uncategorized' }}
-            </span>
-
-            <h2 class="section-title">Description</h2>
-
-            <p class="description">
-                {{ $book->description ?: 'No description available.' }}
-            </p>
-
-            <div class="actions">
-                <a class="button back-button" href="/books">Back to Books</a>
-
-                <a class="button edit-button" href="/books/{{ $book->id }}/edit">Edit</a>
+            <section class="description-block">
+                <h2>Description</h2>
+                <p>{{ $book->description ?: 'No description available.' }}</p>
+            </section>
 
-                <form class="delete-form" method="POST" action="/books/{{ $book->id }}">
+            <div class="detail-actions">
+                <a class="button button--primary" href="/books/{{ $book->id }}/edit">Edit Book</a>
+                <form method="POST" action="/books/{{ $book->id }}">
                     @csrf
                     @method('DELETE')
-
-                    <button class="delete-button" type="submit" onclick="return confirm('Delete this book?')">
-                        Delete
-                    </button>
+                    <button class="button button--danger" type="submit" onclick="return confirm('Delete this book?')">Delete</button>
                 </form>
             </div>
         </div>
-    </div>
-
-</div>
-
-</body>
-</html>
+    </article>
+@endsection
diff --git "a/resources/views/layouts/library.blade.php" "b/resources/views/layouts/library.blade.php"
new file mode 100644
index 0000000..30f2699
--- /dev/null
+++ "b/resources/views/layouts/library.blade.php"
@@ -0,0 +1,29 @@
+<!DOCTYPE html>
+<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
+<head>
+    <meta charset="utf-8">
+    <meta name="viewport" content="width=device-width, initial-scale=1">
+    <title>{{ $title ?? 'Readwise' }}</title>
+    @vite(['resources/css/app.css', 'resources/js/app.js'])
+</head>
+<body class="library-shell">
+    <nav class="site-nav">
+        <div class="site-nav__inner">
+            <a class="brand" href="/books" aria-label="Readwise home">
+                <span class="brand__mark">R</span>
+                <span class="brand__text">Readwise</span>
+            </a>
+
+            <div class="site-nav__links">
+                <a class="nav-link {{ request()->is('/') ? 'is-active' : '' }}" href="/">Home</a>
+                <a class="nav-link {{ request()->is('books') || request()->is('books/*') ? 'is-active' : '' }}" href="/books">Library</a>
+                <a class="button button--primary button--sm" href="/books/create">Add Book</a>
+            </div>
+        </div>
+    </nav>
+
+    <main class="page-wrap">
+        @yield('content')
+    </main>
+</body>
+</html>
diff --git "a/resources/views/welcome.blade.php" "b/resources/views/welcome.blade.php"
index 2cf51a5..b91621a 100644
--- "a/resources/views/welcome.blade.php"
+++ "b/resources/views/welcome.blade.php"
@@ -1,16 +1,17 @@
-<!DOCTYPE html>
-<html>
-<head>
-    <title>READWISE</title>
-</head>
-<body>
+@extends('layouts.library', ['title' => 'Readwise'])
 
-<h1>READWISE</h1>
-
-<p>Website is working.</p>
-
-<a href="/login">Login</a>
-<a href="/register">Register</a>
-
-</body>
-</html>
+@section('content')
+    <section class="welcome-hero">
+        <div class="welcome-hero__content">
+            <p class="eyebrow">Readwise Library</p>
+            <h1>Keep every book in view.</h1>
+            <p>
+                Browse, search, filter, and maintain your personal reading collection from one calm library workspace.
+            </p>
+            <div class="welcome-actions">
+                <a class="button button--primary" href="/books">Open Library</a>
+                <a class="button button--ghost" href="/books/create">Add Book</a>
+            </div>
+        </div>
+    </section>
+@endsection


